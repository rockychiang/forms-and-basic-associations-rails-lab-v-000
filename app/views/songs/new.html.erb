<h1>Create a Song</h1>

<%= form_for @song do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br />

  <%= f.label :genre_name %>
  <%= f.collection_select :genre, Genre.all, :id, :name %>
  <br />

  <%= f.submit %>
<% end %>
